name: PlatformIO CI
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        hw_type: [c3, v3v0, c3v1, c3v2]
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: '3.x'
      - name: Install PlatformIO
        run: pip install platformio

      - name: Build ${{ matrix.hw_type }}
        run: pio run -e esp32-${{ matrix.hw_type }}

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: KeyPass-ESP32-${{ matrix.hw_type }}
          path: .pio/build/esp32-${{ matrix.hw_type }}/*.bin
